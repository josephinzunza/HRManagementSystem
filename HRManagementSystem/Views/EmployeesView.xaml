<Page x:Class="HRManagementSystem.Views.EmployeesView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:HRManagementSystem.Views"
      xmlns:uc="clr-namespace:HRManagementSystem.Controls"
      xmlns:viewModels="clr-namespace:HRManagementSystem.ViewModels"
      xmlns:converters="clr-namespace:HRManagementSystem.Controls.Converters"
    mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="EmployeesView">
    <Page.Resources>
        <converters:ItemCountToVisibilityConverter x:Key="ItemCountToVisibilityConverter" />

    </Page.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.ColumnSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Foreground="Black" FontSize="30" Margin="0,20">People</TextBlock>

        </Grid>
        <uc:SearchBar Grid.Row="1" />
        <Button Grid.Column="1" Grid.Row="1" Margin="5" HorizontalAlignment="Right" VerticalAlignment="Center"
                x:Name="AddPersonButton" Click="AddPersonButton_Click">Add Person</Button>
        <Grid  Grid.ColumnSpan="2" Grid.Row="2">
            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center"
                       >No employees were found.</TextBlock>
            <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding Employees}"
                      Visibility="{Binding Employees.Count, Converter={StaticResource ItemCountToVisibilityConverter}}">
                <DataGrid.Columns>
                    <DataGridTemplateColumn>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Ellipse Width="50" Height="50">
                                    <Ellipse.Fill>
                                        <ImageBrush ImageSource="{Binding ProfilePicture}" Stretch="Uniform" />
                                    </Ellipse.Fill>
                                </Ellipse>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" Width="1*"></DataGridTextColumn>
                    <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" Width="1*"></DataGridTextColumn>
                    <DataGridTemplateColumn Header="Contact Info" Width="2*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition />
                                        <RowDefinition />
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" Text="{Binding Email}"/>
                                    <TextBlock Grid.Row="1" Text="{Binding PhoneNumber}" />
                                </Grid>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Department" Binding="{Binding Department}" Width="1*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</Page>
