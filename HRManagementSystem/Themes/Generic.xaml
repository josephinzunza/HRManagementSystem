<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:HRManagementSystem.Controls"
    xmlns:conv="clr-namespace:HRManagementSystem.Controls.Converters"
    xmlns:theme="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero2">
    <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    <conv:AddLeftPaddingConverter x:Key="AddLeftPaddingConverter" />
    <Style TargetType="{x:Type local:NavButton}">
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:NavButton}">

                    <Border x:Name="background" CornerRadius="8" Padding="{TemplateBinding Padding}" Background="Transparent"
                            BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*" />
                                <ColumnDefinition Width="3*" />
                                <ColumnDefinition Width="1*" />
                            </Grid.ColumnDefinitions>
                            <Path Grid.Column="1" x:Name="icon" Height="{Binding ActualWidth, RelativeSource={RelativeSource Self}}"
                              Stretch="Fill" Data="{TemplateBinding IconGeometry}" Fill="#7b8792" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Fill" TargetName="icon" Value="#2a84f1" />
                            <Setter Property="Background" TargetName="background" Value="#d0ebff" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Fill" TargetName="icon" Value="#2a84f1" />
                            <Setter Property="Background" TargetName="background" Value="#d0ebff" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type local:PlaceholderTextBox}">
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Padding" Value="0 5" />
        <Setter Property="FontFamily" Value="{StaticResource RobotoLight}" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:PlaceholderTextBox}">
                    <Grid>
                        <TextBox x:Name="placeholderTextBox" IsHitTestVisible="False" IsTabStop="False"
                                Text="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=PlaceholderText}"
                                Padding="{TemplateBinding Padding}"
                                Foreground="Gray"
                                FontFamily="{TemplateBinding FontFamily}" FontSize="{TemplateBinding FontSize}" BorderBrush="Transparent" />
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderThickness="1" BorderBrush="Gray" CornerRadius="5">
                            <ScrollViewer Name="PART_ContentHost" VerticalAlignment="Center" />
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource Mode=Self}, Path=IsPlaceholderVisible}"
                                     Value="False">
                            <Setter TargetName="placeholderTextBox" Property="Foreground" Value="Transparent" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="#F0F0F0" />
                <Setter Property="PlaceholderText" Value="" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="{x:Type local:ValidationTextBox}">
        <Setter Property="Padding" Value="0 5" />
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="FontFamily" Value="{StaticResource RobotoLight}" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Validation.ErrorTemplate" Value="{x:Null}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Height" Value="Auto" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ValidationTextBox}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <TextBox IsHitTestVisible="False" IsTabStop="False" VerticalAlignment="Center"
                                 Text="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=PlaceholderText}"
                                 Padding="{TemplateBinding Padding}" Margin="{TemplateBinding Margin}"
                                 Foreground="Gray" SnapsToDevicePixels="True"
                                 FontFamily="{TemplateBinding FontFamily}" FontSize="{TemplateBinding FontSize}" BorderBrush="Transparent"
                                 Visibility="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=IsPlaceholderVisible,
                                              Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderThickness="{TemplateBinding BorderThickness}" 
                                BorderBrush="Gray" CornerRadius="5" Margin="{TemplateBinding Margin}" Padding="{TemplateBinding Padding}">
                            <ScrollViewer Name="PART_ContentHost" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock x:Name="hintTextBlock" Grid.Row="1" Foreground="Red"
                                   TextElement.FontFamily="{TemplateBinding FontFamily}"
                                   TextElement.FontSize="12" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Validation.HasError" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="Red" />
                            <Setter TargetName="hintTextBlock" Property="Text"
                                    Value="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=(Validation.Errors)[0].ErrorContent}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="#F0F0F0" />
                <Setter Property="PlaceholderText" Value="" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="{x:Type local:ValidationPhoneNumberTextBox}">
        <Setter Property="PlaceholderText" Value="(000) 000-0000" />
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Padding" Value="0 5" />
        <Setter Property="FontFamily" Value="{StaticResource RobotoLight}" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Validation.ErrorTemplate" Value="{x:Null}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ValidationPhoneNumberTextBox}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <TextBox IsHitTestVisible="False" IsTabStop="False" VerticalAlignment="Center"
                                 Text="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=PlaceholderText}"
                                 Padding="{TemplateBinding Padding}" Margin="{TemplateBinding Margin}"
                                 Foreground="Gray"
                                 FontFamily="{TemplateBinding FontFamily}" FontSize="{TemplateBinding FontSize}" BorderBrush="Transparent"
                                 Visibility="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=IsPlaceholderVisible,
                                              Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderThickness="{TemplateBinding BorderThickness}" 
                                BorderBrush="Gray" CornerRadius="5" Margin="{TemplateBinding Margin}" Padding="{TemplateBinding Padding}">
                            <ScrollViewer Name="PART_ContentHost" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock x:Name="hintTextBlock" Grid.Row="1" Foreground="Red"
                                   TextElement.FontFamily="{TemplateBinding FontFamily}"
                                   TextElement.FontSize="12" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Validation.HasError" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="Red" />
                            <Setter TargetName="hintTextBlock" Property="Text"
                                    Value="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=(Validation.Errors)[0].ErrorContent}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="#F0F0F0" />
                <Setter Property="PlaceholderText" Value="" />
            </Trigger>
        </Style.Triggers>
    </Style>
    
    <ControlTemplate x:Key="comboBoxErrorTemplate">
        <Border BorderBrush="Red" BorderThickness="1" CornerRadius="5">
            <AdornedElementPlaceholder />
        </Border>
    </ControlTemplate>
    
    <Style TargetType="{x:Type local:ValidationComboBox}">
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="FontFamily" Value="{StaticResource RobotoLight}" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Padding" Value="0 5" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ValidationComboBox}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <ComboBox x:Name="comboBox" Grid.Row="0" IsEditable="True" IsReadOnly="True"
                                      Validation.ValidationAdornerSiteFor="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}}"
                                      FontFamily="{TemplateBinding FontFamily}" FontSize="{TemplateBinding FontSize}" VerticalContentAlignment="Center"
                                      ItemsSource="{TemplateBinding ItemsSource}" SelectedItem="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=SelectedItem}"
                                      SelectedIndex="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=SelectedIndex}"
                                      DisplayMemberPath="{TemplateBinding DisplayMemberPath}" Style="{DynamicResource RoundedComboBox}"
                                      Padding="{TemplateBinding Padding}" Margin="{TemplateBinding Margin}" 
                                      Validation.ErrorTemplate="{StaticResource comboBoxErrorTemplate}" />
                        <TextBlock x:Name="hintTextBlock" Grid.Row="1" Foreground="Red"
                                       TextElement.FontFamily="{TemplateBinding FontFamily}"
                                       TextElement.FontSize="12" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Validation.HasError" Value="True">
                            <Setter TargetName="hintTextBlock" Property="Text"
                                    Value="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=(Validation.Errors)[0].ErrorContent}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type local:ValidationDateTextBox}">
        <Setter Property="PlaceholderText" Value="MM/DD/YYYY" />
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Padding" Value="0 5" />
        <Setter Property="FontFamily" Value="{StaticResource RobotoLight}" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Validation.ErrorTemplate" Value="{x:Null}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ValidationDateTextBox}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <TextBox IsHitTestVisible="False" IsTabStop="False" VerticalAlignment="Center"
                                 Text="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=CurrentPlaceholder}"
                                 Padding="{TemplateBinding Padding}" Margin="{TemplateBinding Margin}"
                                 Foreground="Gray"
                                 FontFamily="{TemplateBinding FontFamily}" FontSize="{TemplateBinding FontSize}" BorderBrush="Transparent"
                                 Visibility="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=IsPlaceholderVisible,
                                              Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderThickness="{TemplateBinding BorderThickness}" 
                                BorderBrush="Gray" CornerRadius="5" Margin="{TemplateBinding Margin}" Padding="{TemplateBinding Padding}">
                            <ScrollViewer Name="PART_ContentHost" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock x:Name="hintTextBlock" Grid.Row="1" Foreground="Red"
                                   TextElement.FontFamily="{TemplateBinding FontFamily}"
                                   TextElement.FontSize="12" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Validation.HasError" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="Red" />
                            <Setter TargetName="hintTextBlock" Property="Text"
                                    Value="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=(Validation.Errors)[0].ErrorContent}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
